{
  "name": "api",
  "version": "1.0.0",
  "description": "The api folder consists of a configuration folder which can be replaced with an .env file. the controllers consist of all the handlers that take care of the incoming requests the data layer handles the logic behind each controller the middlewares validate the incoming requests The models consist of the schemas The routers take care of binding the mentioned route with the specific controller ## End of explanation.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon ./src/index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "@types/jsonwebtoken": "^9.0.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.1.0",
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.1"
  }
}
